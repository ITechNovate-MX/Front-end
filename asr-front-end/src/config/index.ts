const Config = {
    API_URL: 'http://52.90.133.45/:8080/', // Base URL del backend
    FRONT_URL: 'http://localhost:3000/',  // URL base del frontend (para redirecciones o referencias locales)
};

export default Config;